import React, { useState } from 'react';

const Navbar = ({ currentPage, handlePageChange }) => {
    return (
        <header>
            <h1>Lily Thomas</h1>
            <nav>
                
            </nav>
        </header>
    );
};
    
export default Navbar;

    